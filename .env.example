# Chronos Claims — environment template
# Copy to .env.local for local development (optional today).
# None of these variables are read by the app yet.

# Public app URL (future: share links, redirects, OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Future backend / integrations (not used in MVP) ---

# DATABASE_URL=
# S3_BUCKET=
# OPENAI_API_KEY=
# CLERK_SECRET_KEY=
